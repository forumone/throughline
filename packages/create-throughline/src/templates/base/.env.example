# --- Required ---
DATABASE_URI=postgres://user:pass@host/dbname
PAYLOAD_SECRET=generate-with-openssl-rand-base64-48
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# --- Inngest ---
# Leave blank in local dev; use the inngest dev server (npx inngest-cli dev).
# Set both in production after creating an Inngest app.
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# --- Resend (for approval + form notification emails) ---
RESEND_API_KEY=
EMAIL_FROM_ADDRESS=notifications@example.com
EMAIL_FROM_NAME={{projectName}}
EMAIL_REPLY_TO=

# --- Approval tokens ---
# Generate: openssl rand -base64 48
APPROVAL_TOKEN_SECRET=

# --- Forms ---
# Generate: openssl rand -base64 48
FORMS_IP_HASH_SECRET=

# --- MCP server keys ---
# Each key is generated in the Payload admin (API Keys collection) after
# first deploy and pasted back here for local dev.
COMPONENT_SERVER_API_KEY=
PUBLISHING_SERVER_API_KEY=
APPROVALS_SERVER_API_KEY=
AUDIT_SERVER_API_KEY=
FORMS_SERVER_API_KEY=
INTEGRATIONS_SERVER_API_KEY=

# System API key used by scheduled workflows that talk back to the
# Publishing MCP server (executes scheduled publishes).
PUBLISHING_SYSTEM_API_KEY=

# --- Optional: storage ---
# Vercel Blob token if deploying to Vercel; remove this and the storage
# adapter from payload.config.ts if you're using a different provider.
BLOB_READ_WRITE_TOKEN=
