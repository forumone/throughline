// This file is a placeholder. Bring your own design system here:
// - Vendor your component source under `./components/` and re-export from here
// - Or publish the DS separately and replace this package with the consumer
//
// The Components MCP server reads a manifest URL from your payload.config.ts
// (see `componentsPlugin({ manifest })`). Update that URL to point at your
// design system's manifest endpoint once you have one.
export {}
