// Your design system's public surface. Export your components from here as you
// build them under `./components/`, then write a contract (`.contract.ts`) and
// a Storybook story (`.stories.tsx`) for each so the Components MCP server can
// reason about them.
//
// The web app's Components MCP server reads a manifest. Point it at your
// manifest in `apps/web/src/payload.config.ts` (`componentsPlugin({ manifest })`)
// once you generate one.
export {}
